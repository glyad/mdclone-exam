<UserControl x:Class="MdClone.Presentation.Views.TableScreen.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:MdClone.Presentation.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModels:EmailScreenViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding Path=DisplayName, Mode=OneWay}" />
    </Grid>
</UserControl>
